version: '3.8'

services:
  selenium:
    # image: selenium/standalone-chrome:latest
    image: seleniarm/standalone-chromium:latest
    ports:
      - "4444:4444"
    restart: always

  my_app:
    image: princesharma2899/clc3.tech:latest
    depends_on:
      - selenium
    restart: always
